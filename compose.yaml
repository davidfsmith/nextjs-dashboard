version: '3'
name: next-js
services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - 5000:3000
    environment:
      - WDS_SOCKET_PORT=5000
    volumes:
      - ./:/app
      - /app/node_modules
